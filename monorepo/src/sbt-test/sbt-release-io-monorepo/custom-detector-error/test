# Test: custom change detector error is caught and project conservatively treated as changed
$ exec git init
$ exec git config user.email "test@example.com"
$ exec git config user.name "Test User"
$ exec git add .
$ exec git commit -m "Initial commit"

# Release with NO explicit project selection — relies on custom change detector.
# Detector throws for api, succeeds (returns true) for core.
# Api should be conservatively treated as changed and released too.
> releaseIOMonorepo with-defaults release-version core=1.0.0 next-version core=1.1.0-SNAPSHOT release-version api=1.0.0 next-version api=1.1.0-SNAPSHOT

# Verify BOTH projects were tagged (api conservatively treated as changed)
> checkTags
> checkCoreVersion
> checkApiVersion
