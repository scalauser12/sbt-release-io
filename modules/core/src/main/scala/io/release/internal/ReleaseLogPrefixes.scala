package io.release.internal

/** Bracketed log prefixes for release-io core and monorepo plugins. */
private[release] object ReleaseLogPrefixes {
  val Core: String     = "[release-io]"
  val Monorepo: String = "[release-io-monorepo]"
}
