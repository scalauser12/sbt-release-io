# Initialize a local repo and stage project files.
$ exec git init
$ exec git config user.email "test@example.com"
$ exec git config user.name "Test User"
$ exec git add .gitignore build.sbt version.sbt project/plugins.sbt
$ exec git commit -m "Initial commit"
$ exec git branch -M main

# Set up a bare remote with `tagOpt = --no-tags` so the warmup fetch does not
# auto-pull the conflicting tag.
$ exec mkdir remotes
$ exec git init --bare remotes/origin.git
$ exec git remote add origin remotes/origin.git
$ exec git config remote.origin.tagOpt --no-tags
$ exec git push -u origin main

# Create the conflicting v0.1.0 tag from a separate clone.
$ exec git clone remotes/origin.git temp-clone
$ exec git -C temp-clone config user.email "other@example.com"
$ exec git -C temp-clone config user.name "Other Author"
$ exec git -C temp-clone tag v0.1.0
$ exec git -C temp-clone push origin v0.1.0

# Confirm the local repo does NOT see the tag.
-$ exec git rev-parse --verify --quiet refs/tags/v0.1.0

# The build configures a `beforeTag` hook, which auto-disables `tag-preflight`
# in `CoreLifecycle.tagPreflightEnabled`. Without the late probe at
# `tag-release.execute`, the release would mutate version.sbt, create the
# release commit, run the hook, create the local tag, and only fail at the
# final atomic push. With the fix, `tag-release.execute` runs the remote
# probe before creating the tag and aborts cleanly.
-> releaseIO with-defaults release-version 0.1.0 next-version 0.2.0-SNAPSHOT

# Verify the release commit landed but the late probe blocked tag creation.
> checkNoTagOrPublishSideEffects
