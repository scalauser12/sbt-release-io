# No remote configured — push policy ENABLED but the operator declined via
# `releaseIODefaultsPushAnswer := Some(false)`. The push step's validate and
# execute short-circuit through `onDeclinePush`. The beforePush hook would
# have fired before the narrow gate; this asserts the gate suppresses it.
$ exec git init
$ exec git config user.email "test@example.com"
$ exec git config user.name "Test User"
$ exec git add .gitignore build.sbt version.sbt project/plugins.sbt
$ exec git commit -m "Initial commit"

> releaseIO with-defaults release-version 0.1.0 next-version 0.2.0-SNAPSHOT

$ exec grep -q "0.2.0-SNAPSHOT" version.sbt
> checkBeforePushSuppressed
