# Initialize git repository
$ exec git init
$ exec git config user.email "test@example.com"
$ exec git config user.name "Test User"

# Commit initial version.sbt with SNAPSHOT version
$ exec git add .gitignore build.sbt version.sbt project/plugins.sbt
$ exec git commit -m "Initial commit with version 0.1.0-SNAPSHOT"

# Run release - verify no issues when version.sbt changes from SNAPSHOT to release and back
> releaseIO with-defaults release-version 0.1.0 next-version 0.2.0-SNAPSHOT

# Verify tag was created (using custom task like upstream)
> checkGitTag

# Verify final version is next version
$ exec grep -q "0.2.0-SNAPSHOT" version.sbt
