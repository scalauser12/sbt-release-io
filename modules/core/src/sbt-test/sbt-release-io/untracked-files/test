# Initialize git repository
$ exec git init
$ exec git config user.email "test@example.com"
$ exec git config user.name "Test User"
$ exec git add .gitignore build.sbt version.sbt project/plugins.sbt
$ exec git commit -m "Initial commit"

# Create an untracked file
$ touch untracked.txt

# Verify untracked file exists
$ exists untracked.txt

# Release should succeed despite untracked file (because releaseIOIgnoreUntrackedFiles := true)
> releaseIO with-defaults release-version 0.1.0 next-version 0.2.0-SNAPSHOT

# Verify release completed - check a tag was created (using custom task like upstream)
> checkGitTag

# Verify final version
$ exec grep -q "0.2.0-SNAPSHOT" version.sbt
