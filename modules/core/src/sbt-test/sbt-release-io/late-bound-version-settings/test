$ exec git init
$ exec git config user.email "test@example.com"
$ exec git config user.name "Test User"
$ exec git add .gitignore build.sbt version.sbt version.properties project/plugins.sbt project/LateBoundVersionPlugin.scala
$ exec git commit -m "Initial commit"

> releaseLateBoundVersion with-defaults release-version 0.1.0 next-version 0.2.0-SNAPSHOT

$ exists late-bound-version-settings-ran
> checkRuntimeVersionFile
