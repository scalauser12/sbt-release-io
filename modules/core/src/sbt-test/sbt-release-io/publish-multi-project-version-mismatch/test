$ exec git init
$ exec git config user.email "test@example.com"
$ exec git config user.name "Test User"
$ exec git add .
$ exec git commit -m "Initial commit"

# The core command has one release version. A publishable aggregate child with its own version
# must fail during validation, before version writes, commits, tags, or publish actions.
-> releaseIO with-defaults release-version 0.1.0 next-version 0.2.0-SNAPSHOT
> checkRejectedBeforeMutation
