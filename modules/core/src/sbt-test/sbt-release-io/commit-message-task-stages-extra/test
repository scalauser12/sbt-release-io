# Initialize git repository
$ exec git init
$ exec git config user.email "test@example.com"
$ exec git config user.name "Test User"
$ exec git add build.sbt version.sbt project/plugins.sbt
$ exec git commit -m "Initial commit"

# The commit-message task stages an unrelated file. The post-task recheck must
# reject the commit before tagging so the unrelated change cannot ride along.
-> releaseIO with-defaults release-version 0.1.0 next-version 0.2.0-SNAPSHOT

> checkNoReleaseTag
> checkInitialHeadOnly
