# Test: pre-existing unified tag causes release failure
$ exec git init
$ exec git config user.email "test@example.com"
$ exec git config user.name "Test User"
$ exec git add .
$ exec git commit -m "Initial commit"

# Pre-create the unified tag that will conflict
$ exec git tag v1.0.0

# Release should fail because v1.0.0 unified tag already exists
-> releaseIOMonorepo core api with-defaults release-version core=1.0.0 next-version core=1.1.0-SNAPSHOT release-version api=1.0.0 next-version api=1.1.0-SNAPSHOT
> checkFailureArtifacts
