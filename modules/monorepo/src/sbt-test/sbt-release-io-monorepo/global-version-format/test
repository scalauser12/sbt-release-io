# Test: global version mode writes "ThisBuild / version" format (not plain "version :=")
$ exec git init
$ exec git config user.email "test@example.com"
$ exec git config user.name "Test User"
$ exec git add .
$ exec git commit -m "Initial commit"

# Release with global version overrides
> releaseIOMonorepo core api with-defaults release-version 1.0.0 next-version 1.1.0-SNAPSHOT

# Verify the version file uses "ThisBuild / version" format
> checkVersionFileFormat
