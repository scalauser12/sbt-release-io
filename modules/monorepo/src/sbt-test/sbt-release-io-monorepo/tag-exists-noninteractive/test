# Test: per-project tag collision in non-interactive mode (no with-defaults)
# Exercises the !ctx.interactive path in createTag (MonorepoVcsSteps line 108)
$ exec git init
$ exec git config user.email "test@example.com"
$ exec git config user.name "Test User"
$ exec git add .
$ exec git commit -m "Initial commit"

# Create a tag that will conflict with the release
$ exec git tag core/v1.0.0

# Release WITHOUT with-defaults — non-interactive is the default
-> releaseIOMonorepo core api release-version core=1.0.0 next-version core=1.1.0-SNAPSHOT release-version api=1.0.0 next-version api=1.1.0-SNAPSHOT

# Verify: pre-existing core tag preserved, api tag created (isolation), next-version skipped
> checkAll
