# Test: custom write function combined with global version mode
$ exec git init
$ exec git config user.email "test@example.com"
$ exec git config user.name "Test User"
$ exec git add .
$ exec git commit -m "Initial commit"

# Run monorepo release with global version overrides
> releaseIOMonorepo core api with-defaults release-version 1.0.0 next-version 1.1.0-SNAPSHOT

# Verify custom format preserved, versions correct, and git history intact
> checkAll
