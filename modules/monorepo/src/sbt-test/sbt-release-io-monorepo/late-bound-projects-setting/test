$ exec git init
$ exec git config user.email "test@example.com"
$ exec git config user.name "Test User"
$ exec git add .gitignore build.sbt core/version.sbt api/version.sbt project/plugins.sbt project/LateBoundProjectsPlugin.scala
$ exec git commit -m "Initial commit"

> releaseLateBoundProjects all-changed with-defaults release-version core=1.0.0 next-version core=1.1.0-SNAPSHOT

$ exists late-bound-projects-setting-ran
> checkOnlyCoreReleased
