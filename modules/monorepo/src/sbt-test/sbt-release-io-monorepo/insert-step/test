# Test: insertStepAfter and insertStepBefore correctly position custom steps
$ exec git init
$ exec git config user.email "test@example.com"
$ exec git config user.name "Test User"
$ exec git add .
$ exec git commit -m "Initial commit"

# Marker files should not exist before release
$ absent marker/after-set-version
$ absent marker/before-tag

# Release
> releaseIOMonorepo core with-defaults release-version core=1.0.0 next-version core=1.1.0-SNAPSHOT

# Verify both custom steps executed
$ exists marker/after-set-version
$ exists marker/before-tag
