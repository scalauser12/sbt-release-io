# Test: skipPublish setting bypasses publishTo check and publish action
$ exec git init
$ exec git config user.email "test@example.com"
$ exec git config user.name "Test User"
$ exec git add .
$ exec git commit -m "Initial commit"

# Release with publish-artifacts step present but skipPublish=true.
# Without skipPublish, check phase would fail since publishTo is not set.
> releaseIOMonorepo core api with-defaults release-version core=0.1.0 next-version core=0.2.0-SNAPSHOT release-version api=0.1.0 next-version api=0.2.0-SNAPSHOT

> checkAll
