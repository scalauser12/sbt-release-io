# Test: releaseIOMonorepoPublishArtifactsChecks := false bypasses publishTo validation
$ exec git init
$ exec git config user.email "test@example.com"
$ exec git config user.name "Test User"
$ exec git add .
$ exec git commit -m "Initial commit"

# Release should succeed despite missing publishTo (checks disabled)
> releaseIOMonorepo core with-defaults release-version core=0.1.0 next-version core=0.2.0-SNAPSHOT

# Verify the release completed successfully
> checkAll
