# Test: combining all-changed with explicit project selection should fail
$ exec git init
$ exec git config user.email "test@example.com"
$ exec git config user.name "Test User"
$ exec git add .
$ exec git commit -m "Initial commit"

# all-changed + explicit project name should be rejected
-> releaseIOMonorepo all-changed core with-defaults release-version core=1.0.0 next-version core=1.1.0-SNAPSHOT
