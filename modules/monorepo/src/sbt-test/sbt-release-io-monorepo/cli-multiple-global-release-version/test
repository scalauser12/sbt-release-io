# Test: multiple global release-version overrides should fail validation
$ exec git init
$ exec git config user.email "test@example.com"
$ exec git config user.name "Test User"
$ exec git add .
$ exec git commit -m "Initial commit"

# Two global release-version flags should be rejected
-> releaseIOMonorepo core with-defaults release-version 1.0.0 release-version 2.0.0 next-version 1.1.0-SNAPSHOT
